services:

  mysql:
    image: mysql:8.0
    container_name: coin-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: coin_db
      MYSQL_USER: coin_user
      MYSQL_PASSWORD: coin_pass
    ports:
      - "3307:3307"
    volumes:
      - mysql-data:/var/lib/mysql

  redis:
    image: redis:7
    container_name: coin-redis
    ports:
      - "6379:6379"

volumes:
  mysql-data: