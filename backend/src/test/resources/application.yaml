spring:
  application:
    name: simulator
  datasource:
    url: jdbc:tc:mysql:8.0.33:///coin_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

    jpa:
      hibernate:
        ddl-auto: create
      show-sql: true
      properties:
        hibernate:
          format_sql: true